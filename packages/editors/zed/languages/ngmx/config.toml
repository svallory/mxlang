name = "AngularMX"
grammar = "solidmx"
path_suffixes = ["ng.mx"]

# TypeScript host language: line comments, no HTML comments — everything
# outside an `mx_element` region is plain TypeScript, unlike Marko's own
# markup-mode host. Same convention as SolidMX; this grammar (a patched
# tree-sitter-typescript tsx dialect) has no Angular- or Solid-specific
# behavior, so AngularMX reuses the solidmx grammar under its own language
# name and path suffix rather than compiling a second grammar package.
line_comments = ["// "]
block_comment = ["/* ", " */"]

tab_size = 2
hard_tabs = false

autoclose_before = ";:.,=}])>` \t\n"

brackets = [
  { start = "<", end = ">", close = true, newline = false },
  { start = "{", end = "}", close = true, newline = true },
  { start = "[", end = "]", close = true, newline = true },
  { start = "(", end = ")", close = true, newline = true },
  { start = "\"", end = "\"", close = true, newline = false, not_in = ["string", "comment"] },
  { start = "'", end = "'", close = true, newline = false, not_in = ["string", "comment"] },
  { start = "`", end = "`", close = true, newline = false, not_in = ["string", "comment"] },
]
