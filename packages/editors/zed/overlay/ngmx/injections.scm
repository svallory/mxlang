; AngularMX-specific injection overlay, concatenated onto
; base/ngmx/injections.scm by scripts/vendor.sh. Empty for now: the
; `marko` injection into `mx_element` already covers the only embedded
; region this grammar produces.
